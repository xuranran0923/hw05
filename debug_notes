# 调试笔记（Debug Notes）

## 问题1：import torch 报错
错误信息：
OSError: [WinError 1114] 动态链接库(DLL)初始化例程失败。

原因：
Windows 系统缺少运行 PyTorch 所需的 Microsoft Visual C++ 运行库。

解决方法：
下载并安装微软官方 VC_redist.x64.exe，安装完成后重启电脑，问题解决。

---

## 问题2：虚拟环境无法正常加载 torch
错误表现：
在 .venv 虚拟环境中运行代码仍然报错 DLL 加载失败。

原因：
虚拟环境权限与系统库不兼容。

解决方法：
退出虚拟环境（deactivate），使用系统全局 Python 运行代码，成功解决。

---

## 问题3：数据集下载
MNIST 数据集第一次运行时自动下载，路径为 ./data，无报错，下载正常。

---

## 问题4：训练过程中损失数值较大
现象：
训练损失显示为几十、几百，不是 0.xx 格式。

原因：
代码中统计的是一轮所有样本的总损失，不是平均损失，属于正常现象。

判断标准：
损失持续下降、准确率不断上升，说明模型训练正常收敛。

---

## 最终结果
极简 CNN 和 LeNet-5 均能正常训练、收敛，测试准确率达到 98% 以上，所有功能正常运行。
